function inputToArray(input) {
    if (typeof input !== 'string' && input !== 'string') {
        throw new Error('Must be a string or a number.');
    }
    switch (typeof number) {
        case 'string':
            inputAsArray = Array.from(input);
            return inputAsArray;
        case 'number':
            input.toString();
    }       
}